# PhotonEdge Web Application Dependencies
# Install: pip install -r requirements-webapp.txt

# Core (same as requirements.txt)
numpy>=1.24.0
scipy>=1.10.0
scikit-image>=0.20.0
matplotlib>=3.7.0

# Web framework
flask>=3.0.0
flask-socketio>=5.3.0

# Image processing (headless for server)
opencv-python-headless>=4.8.0

# Optional: Object detection (uncomment to enable)
# ultralytics>=8.0.0

# Optional: Depth estimation (uncomment to enable)
# torch>=2.0.0
# torchvision>=0.15.0
# timm>=0.9.0
